srcdir = @srcdir@
VPATH  = @srcdir@

CC = @HOSTCC@

CFLAGS += -Wall -O2 -I$(srcdir)/../libmspack -I../libmspack

unpack: unpack.o ../libmspack/libmspack.a
	$(CC) -o $@ $^

clean:
	rm -f unpack.o unpack
